<div class="page-header">
    <h1>Heroes</h1>
</div>
  <%= paginate @heroes %>
  <ul>
    <% @heroes.each do |hero| %>
    <li><%= link_to hero.name, hero_path(hero) %></li>
  <% end %>
  </ul>
  <br>
  <br>
  <br>
